# Vosk Model

The local voice command workflow expects the Vosk small English model here:

```bash
voice_control/models/vosk-model-small-en-us-0.15
```

The model binaries are not committed because they are large. Download and extract the model on the robot machine before running the local voice workflow.
